#include <stdio.h>
int main(int argc, char const *argv[])
{
	int odd = 0；
	int even = 0；

	int number;
	scanf("%d",&number);
	while(number != -1){
		if (number / 2 == 0){
			even++;
		}
		else{
			odd++;
		}
		scanf("%d",&number);
	}

	printf("%d %d", odd, even);
	return 0;
}